services:
  nginx:
    build: ./requirements/nginx
    container_name: nginx
    ports:
      - "443:443"
    networks:
      - inception_network
    restart: always

networks:
  inception_network:
    driver: bridge